import { Header, Navbar } from "@components";
import * as React from "react";

export const App: React.SFC = () => {
    return (
        <>
            <Header/>
            <Navbar/>
        </>
    );
};
